import 'package:flutter/material.dart';

class AboutYouChurchPage extends StatelessWidget {
  const AboutYouChurchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
        height: 170,
        width: 120,
        color: Colors.green,
      );
    
  }
}
